# Naruchan Project Agent Rules

このリポジトリでは、なるちゃんプロジェクト専用エージェントとして動作する。
最優先事項は、購入履歴、犬舎情報、体重記録、医療記録、トレーニング記録の継続管理とデータ保全である。

## 起動時の優先ルール

- 起動時はこの `AGENTS.md` を優先して読み込む。
- このプロジェクトでは、なるちゃん関連データの管理エージェントとして振る舞う。
- 既存データの削除は禁止する。
- 更新は原則として追記方式で行う。
- 不明な情報は推測で確定せず、必要に応じて `要確認` と記録する。
- 画像やユーザー報告から抽出できた情報のみを確定情報として扱う。

## 管理対象ファイル

主な更新対象は以下のファイルとする。

- `data/purchases.csv`
- `data/medical.csv`
- `data/weight.csv`
- `data/training.csv`
- `naruchan_data.json`

関連ファイルとして、必要に応じて以下も確認する。

- `README.md`
- `docs/project-plan.md`
- `purchases.json`
- `photos/`
- `incoming/`

## 画像確認ルール

`incoming/` および `photos/` 配下の新規画像を確認する。
`incoming/` が存在しない場合は、その旨を報告し、存在する画像フォルダだけ確認する。
ユーザーが「写真整理して」または「inbox処理して」と指示した場合は、`photos/inbox/` 内の写真を確認し、確認を求めずに写真整理手順を自動実行する。

画像から確認する情報は以下とする。

- 商品名
- 購入価格
- 注文日
- 犬舎情報
- 体重情報
- 医療情報
- トレーニングや行動に関する情報

推定できる情報は提案に留める。
確実に読み取れない情報は `要確認` として扱う。

## 写真管理ルール

`photos/inbox/` に配置された写真は、内容と撮影日または更新日を確認して、以下の分類先へ移動する。
日付は可能な限り写真のEXIF撮影日を優先し、読み取れない場合はファイルの更新日を使う。

- お迎え関連: `photos/YYYY/MM/DD/arrival/`
  - 初対面
  - 車移動
  - 自宅到着
  - 記念撮影
- 庭: `photos/YYYY/MM/DD/garden/`
  - 庭遊び
  - 探索
  - 日向ぼっこ
- ひまわり: `photos/YYYY/MM/DD/sunflower/`
  - 発芽
  - 成長記録
  - 花
- 室内: `photos/YYYY/MM/DD/indoor/`
  - クレート
  - ソファ
  - お昼寝
  - おもちゃ
- 散歩: `photos/YYYY/MM/DD/walk/`
  - 初散歩
  - 公園
  - 外出
- 病院: `photos/YYYY/MM/DD/vet/`
  - 動物病院
  - 診察
  - 体重測定

分類に迷う場合は、写真から最も確実に読み取れる内容を優先する。
確定できない場合は `photos/YYYY/MM/DD/indoor/` など安全な分類先へ入れ、日記や報告に `要確認` と記録する。

## 書類写真管理ルール

書類写真またはスキャン画像は `medical/documents/` 配下へ分類する。

- ワクチン証明書: `medical/documents/vaccine/`
- 血統書: `medical/documents/pedigree/`
- マイクロチップ書類: `medical/documents/microchip/`
- 保険書類: `medical/documents/insurance/`
- 領収書: `medical/documents/receipts/`

書類内容を読み取れる場合は、必要に応じて `data/medical.csv`、`medical/vaccine-record.md`、`medical/veterinary-visits.md`、購入履歴関連ファイルも更新する。

## 写真重複判定ルール

重複写真を検出した場合は、削除せず、削除候補として一覧表示する。
保持候補は以下の優先順位で選ぶ。

1. 最新更新日時の写真
2. 解像度が高い写真
3. ファイルサイズが大きい写真

報告例:

```text
Duplicate found:
- IMG_1001.JPG
- IMG_1001(1).JPG

Keeping:
IMG_1001(1).JPG

Removing candidate:
IMG_1001.JPG
```

## お気に入り写真ルール

以下に該当する写真は、元写真を分類先へ移動したうえで `photos/favorites/` にコピーする。

- 表情が良い
- 成長記録として重要
- 初体験イベント
- 家族写真
- SNSアイコン候補

お気に入り選定は写真内容から判断し、判断理由を結果レポートに短く記録する。

## 写真整理後の日記更新

写真整理後は `daily-log/YYYY-MM-DD.md` を作成または更新する。
既存ファイルがある場合は追記し、既存内容は削除しない。

記録内容:

- 当日の出来事
- 発見した行動
- トイレ状況
- 食事状況
- 写真から分かる内容
- 不明点または `要確認` の事項

写真から確認できない項目は推測せず、必要に応じて `要確認` と記録する。

## inbox写真整理手順

ユーザーが「写真整理して」または「inbox処理して」と指示したら、以下を実行する。

1. `photos/inbox/` 内の写真確認
2. 写真内容と書類内容の解析
3. 必要なフォルダ作成
4. 写真または書類画像の移動
5. 重複候補の検出
6. `photos/favorites/` へのコピー
7. `daily-log/YYYY-MM-DD.md` の作成または更新
8. 結果レポート出力

## 購入履歴管理ルール

ユーザーが以下を提供した場合は、購入品候補として処理する。

- 商品画像
- Amazon注文画面
- レシート画像
- 商品写真
- 購入報告

購入履歴で抽出する項目は以下とする。

- 日付
- 商品名
- カテゴリ
- 金額
- 数量
- メモ

可能であれば、店舗、画像パス、レシートパス、到着状況、到着日も記録する。

カテゴリ例:

- クレート
- ソフトクレート
- リード
- カラー
- 食器
- フード
- おやつ
- カメラ
- SDカード
- 庭設備
- 犬用品
- その他

既存CSVに近いカテゴリ名がある場合は、既存データとの整合性を優先する。

## 重複防止ルール

CSVへ追加する前に既存データを確認する。

以下が一致する場合は重複候補と判断する。

- 商品名
- 日付
- 金額

重複候補は追加しない。
商品名に表記揺れがある場合は、明らかに同一商品と判断できるときのみ重複として扱う。
判断が難しい場合は、追加前に `要確認` として報告する。

## CSV更新ルール

- 既存データは削除しない。
- 既存行を書き換える場合は、到着状況の反映など明確な更新理由がある場合に限る。
- 追記時は既存のCSVヘッダーと列順を維持する。
- IDは既存の最大IDから連番で採番する。
- 空欄が必要な列は空欄のまま残し、CSV形式を壊さない。
- 金額は数値で記録し、通貨記号は入れない。
- 日付は `YYYY-MM-DD` 形式で記録する。

## JSON更新ルール

- `naruchan_data.json` はCSV更新に合わせて必要な範囲だけ更新する。
- `last_updated` は実際にデータ更新した日付に更新する。
- 購入履歴を追加した場合は `purchase_history` と `total_purchase_amount` の整合性を確認する。
- JSON形式を維持し、既存の無関係なデータは変更しない。

## 医療・体重・トレーニング記録

医療記録は `data/medical.csv` に記録する。

- ワクチン
- 狂犬病
- ノミダニ予防
- 通院
- 処方
- 体調変化

体重記録は `data/weight.csv` に記録する。

- 日付
- 体重kg
- メモ

トレーニング記録は `data/training.csv` に記録する。

- 日付
- 内容
- 結果
- メモ

ファイルが空、またはヘッダーが不足している場合は、既存方針に合わせて最小限のヘッダーを整備する。

## 自動更新手順

新規画像または購入報告を検出した場合は、以下の順序で処理する。

1. 内容解析
2. 既存データ確認
3. 重複判定
4. 購入・医療・体重・トレーニング情報の抽出
5. CSV更新
6. `naruchan_data.json` 更新
7. `git status` で変更確認
8. `git add .`
9. `git commit -m "Update Naruchan records"`
10. `git push`

変更がない場合は、コミットとプッシュは実行しない。

## Git運用

データ更新が完了した場合は以下を実行する。

```bash
git add .
git commit -m "Update Naruchan records"
git push
```

作業前後に `git status --short` を確認する。
未関係なユーザー変更が存在する場合は削除・巻き戻しをしない。
このプロジェクトのデータ保全を優先し、破壊的なGit操作は行わない。

## 報告形式

更新後は必ず以下の形式で報告する。

```text
追加件数:
更新ファイル:
追加内容:
コミットID:
Push結果:
```

変更がない場合も、追加件数を `0件` として報告し、コミットやPushを実行しなかった理由を明記する。

## データ保全の原則

- 既存データの削除は禁止。
- 推測情報を確定データとして記録しない。
- 不明情報は `要確認` とする。
- 重複登録を避ける。
- CSVとJSONの整合性を保つ。
- 更新前に対象ファイルを確認する。
- 更新後にGitで保存し、リモートへPushする。
