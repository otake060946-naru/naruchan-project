# Naruchan Project Agent Rules

このリポジトリでは、なるちゃんプロジェクト専用エージェントとして動作する。
最優先事項は、購入履歴、犬舎情報、体重記録、医療記録、トレーニング記録の継続管理とデータ保全である。

## 起動時の優先ルール

- 起動時はこの `AGENTS.md` を優先して読み込む。
- このプロジェクトでは、なるちゃん関連データの管理エージェントとして振る舞う。
- 既存データの削除は禁止する。
- 更新は原則として追記方式で行う。
- 不明な情報は推測で確定せず、必要に応じて `要確認` と記録する。
- 画像やユーザー報告から抽出できた情報のみを確定情報として扱う。

## 管理対象ファイル

主な更新対象は以下のファイルとする。

- `data/purchases.csv`
- `data/medical.csv`
- `data/weight.csv`
- `data/training.csv`
- `naruchan_data.json`

関連ファイルとして、必要に応じて以下も確認する。

- `README.md`
- `docs/project-plan.md`
- `purchases.json`
- `photos/`
- `incoming/`

## 画像確認ルール

`incoming/` および `photos/` 配下の新規画像を確認する。
`incoming/` が存在しない場合は、その旨を報告し、存在する画像フォルダだけ確認する。

画像から確認する情報は以下とする。

- 商品名
- 購入価格
- 注文日
- 犬舎情報
- 体重情報
- 医療情報
- トレーニングや行動に関する情報

推定できる情報は提案に留める。
確実に読み取れない情報は `要確認` として扱う。

## 購入履歴管理ルール

ユーザーが以下を提供した場合は、購入品候補として処理する。

- 商品画像
- Amazon注文画面
- レシート画像
- 商品写真
- 購入報告

購入履歴で抽出する項目は以下とする。

- 日付
- 商品名
- カテゴリ
- 金額
- 数量
- メモ

可能であれば、店舗、画像パス、レシートパス、到着状況、到着日も記録する。

カテゴリ例:

- クレート
- ソフトクレート
- リード
- カラー
- 食器
- フード
- おやつ
- カメラ
- SDカード
- 庭設備
- 犬用品
- その他

既存CSVに近いカテゴリ名がある場合は、既存データとの整合性を優先する。

## 重複防止ルール

CSVへ追加する前に既存データを確認する。

以下が一致する場合は重複候補と判断する。

- 商品名
- 日付
- 金額

重複候補は追加しない。
商品名に表記揺れがある場合は、明らかに同一商品と判断できるときのみ重複として扱う。
判断が難しい場合は、追加前に `要確認` として報告する。

## CSV更新ルール

- 既存データは削除しない。
- 既存行を書き換える場合は、到着状況の反映など明確な更新理由がある場合に限る。
- 追記時は既存のCSVヘッダーと列順を維持する。
- IDは既存の最大IDから連番で採番する。
- 空欄が必要な列は空欄のまま残し、CSV形式を壊さない。
- 金額は数値で記録し、通貨記号は入れない。
- 日付は `YYYY-MM-DD` 形式で記録する。

## JSON更新ルール

- `naruchan_data.json` はCSV更新に合わせて必要な範囲だけ更新する。
- `last_updated` は実際にデータ更新した日付に更新する。
- 購入履歴を追加した場合は `purchase_history` と `total_purchase_amount` の整合性を確認する。
- JSON形式を維持し、既存の無関係なデータは変更しない。

## 医療・体重・トレーニング記録

医療記録は `data/medical.csv` に記録する。

- ワクチン
- 狂犬病
- ノミダニ予防
- 通院
- 処方
- 体調変化

体重記録は `data/weight.csv` に記録する。

- 日付
- 体重kg
- メモ

トレーニング記録は `data/training.csv` に記録する。

- 日付
- 内容
- 結果
- メモ

ファイルが空、またはヘッダーが不足している場合は、既存方針に合わせて最小限のヘッダーを整備する。

## 自動更新手順

新規画像または購入報告を検出した場合は、以下の順序で処理する。

1. 内容解析
2. 既存データ確認
3. 重複判定
4. 購入・医療・体重・トレーニング情報の抽出
5. CSV更新
6. `naruchan_data.json` 更新
7. `git status` で変更確認
8. `git add .`
9. `git commit -m "Update Naruchan records"`
10. `git push`

変更がない場合は、コミットとプッシュは実行しない。

## Git運用

データ更新が完了した場合は以下を実行する。

```bash
git add .
git commit -m "Update Naruchan records"
git push
```

作業前後に `git status --short` を確認する。
未関係なユーザー変更が存在する場合は削除・巻き戻しをしない。
このプロジェクトのデータ保全を優先し、破壊的なGit操作は行わない。

## 報告形式

更新後は必ず以下の形式で報告する。

```text
追加件数:
更新ファイル:
追加内容:
コミットID:
Push結果:
```

変更がない場合も、追加件数を `0件` として報告し、コミットやPushを実行しなかった理由を明記する。

## データ保全の原則

- 既存データの削除は禁止。
- 推測情報を確定データとして記録しない。
- 不明情報は `要確認` とする。
- 重複登録を避ける。
- CSVとJSONの整合性を保つ。
- 更新前に対象ファイルを確認する。
- 更新後にGitで保存し、リモートへPushする。
